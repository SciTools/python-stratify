name: stratify-dev

channels:
  - conda-forge

dependencies:
  - python=3.8

# Setup dependencies.
  - setuptools
  - setuptools-scm

# Core dependencies.
  - numpy
  - cython

# Optional dependencies.
  - dask

# Developer dependencies.
  - black
  - flake8
  - isort
  - pip
  - pre-commit

# Test dependencies.
  - codecov
  - pytest
  - pytest-cov
