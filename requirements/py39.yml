name: stratify-dev

channels:
  - conda-forge

dependencies:
  - python=3.9

# Setup dependencies.
  - setuptools
  - setuptools-scm

# Core dependencies.
  - numpy==1.26.4
  - cython

# Optional dependencies.
  - dask

# Developer dependencies.
  - pip
  - pre-commit

# Test dependencies.
  - codecov
  - pytest
  - pytest-cov
