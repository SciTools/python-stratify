name: stratify-dev

channels:
  - conda-forge

dependencies:
  - python=3.14

# Setup dependencies.
  - setuptools>=77.0.3
  - setuptools-scm>=8

# Core dependencies.
  - numpy>=2
  - cython>=3.1.0

# Optional dependencies.
  - dask>=2025.7.0

# Developer dependencies.
  - pip
  - pre-commit

# Test dependencies.
  - codecov
  - pytest
  - pytest-cov
