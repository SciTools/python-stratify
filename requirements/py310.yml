name: stratify-dev

channels:
  - conda-forge

dependencies:
  - python=3.10

# Setup dependencies.
  - setuptools
  - setuptools-scm

# Core dependencies.
  - numpy
  - cython

# Optional dependencies.
  - dask

# Developer dependencies.
  - pip
  - pre-commit

# Test dependencies.
  - codecov
  - pytest
  - pytest-cov
