name: stratify-dev

channels:
  - conda-forge

dependencies:
  - python=3.10

# Setup dependencies.
  - setuptools
  - setuptools-scm

# Core dependencies.
  - numpy>=2
  - cython

# Optional dependencies.
  - dask

# Developer dependencies.
  - pip
  - pre-commit

# Test dependencies.
  - codecov
  - pytest
  - pytest-cov
